---
category: 待整理
tags: [Inbox]
hidden: true   # 收件箱使用说明，移出公开导航（导航/搜索/标签均排除），仓库内保留
---

# 把待整理笔记放这里

新笔记先丢进这个文件夹（或对应领域的 `Inbox/`）。

运行 `python Scripts/classify.py` 后，脚本会根据内容自动把笔记移动到对应领域，并写上 `category` 和 `tags`。
没匹配到领域的笔记会留在这里，等你手动归类。
